A simple program for classification based on band values, with an easy-to-use interface, and available in both Chinese and English versions.
Attention
1 The input is a three-band tif file.
2 All the threshold ranges need to be input completely before the calculation can be performed.
3 After previewing in the preview window, decide whether to output the filtered tif files.

